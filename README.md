# PrimeNumbers

todo

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'prime_numbers'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install prime_numbers
